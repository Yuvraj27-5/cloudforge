/**
 * Deployment lifecycle: deployment records, pipeline executions, events, rollback events.
 *
 * <p>Filled in <strong>Phase 2</strong>. Represents the full lifecycle before any real
 * cloud deployment exists, so the UI and API can be built against it.
 */
package com.cloudforge.backend.deployment;
