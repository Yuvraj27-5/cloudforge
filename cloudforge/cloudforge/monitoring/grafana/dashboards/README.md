# dashboards

**Phase 10.** Dashboard JSON, provisioned as files rather than clicked together in
the UI so they live in version control.

Planned: deployment overview (success rate, rollbacks, average risk score) and
application health (CPU, memory, response time, error rate, restarts).
